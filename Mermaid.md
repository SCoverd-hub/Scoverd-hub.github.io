```mermaid
flowchart TD
	Main([Main]) --> 
	Integer[[Integer secret]] -->
	Integer[[Integer guess]] -->
	Comment brace-r In the classic game, the number ranges from 1 to 100 brace-r -->
	End([End])
```